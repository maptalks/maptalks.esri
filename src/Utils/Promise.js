import { Promise } from 'maptalks';

// let promise;

// if (typeof Promise !== 'undefined') {
//     // built-in Promise
//     promise = Promise;
// } else {
//     promise = Zousan;
// }

export default Promise;